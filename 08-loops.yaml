# loops in ansible 
- name: loops 
  hosts: localhost
  tasks:
    - name: install 
      ansible.builtin.package:
        msg: "{{ item }}"
      loop:
      - git  
      - mysql 
      - java 
      
